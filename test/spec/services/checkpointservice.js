'use strict';

describe('Service: CheckpointService', function () {

  // load the service's module
  beforeEach(module('hyenaCheckpointsApp'));

  // instantiate service
  var CheckpointService;
  beforeEach(inject(function (_CheckpointService_) {
    CheckpointService = _CheckpointService_;
  }));

});
